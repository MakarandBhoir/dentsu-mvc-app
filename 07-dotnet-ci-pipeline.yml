trigger:
- main

stages:
  - stage: 
    displayName: Build
    jobs:
      - job: "BuildProject"
        pool:
          vmImage: ubuntu-latest
        steps:
          - task: DotNetCoreCLI@2
            inputs:
              command: 'build'